<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Weather App</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-100">
    <div class="flex justify-center items-center h-screen">
      <div
        class="container mx-auto px-4 py-8 max-w-sm rounded-xl shadow-lg bg-white"
      >
        <h1 class="text-3xl font-bold mb-4 flex justify-center">Weather App</h1>

        <form action="/weather" method="get" class="mb-8">
          <div class="mb-4">
            <input
              type="text"
              name="city"
              placeholder="Enter city name"
              class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:border-indigo-500 focus:ring focus:ring-indigo-500 focus:ring-opacity-50"
            />
          </div>
          <button
            type="submit"
            class="inline-block bg-blue-500 text-white px-6 py-3 rounded-lg transition duration-300 hover:bg-blue-600"
          >
            Get Weather
          </button>
        </form>

        <% if (error) { %>
        <p class="text-red-500 mb-4"><%= error %></p>
        <% } else if (weather) { %>
        <div class="bg-white rounded-lg shadow-md p-6">
          <h2 class="text-lg font-semibold mb-4">
            Weather in <%= weather.name %>, <%= weather.sys.country %>
          </h2>
          <div class="flex items-center mb-2">
            <i class="fas fa-thermometer-half text-xl text-red-500 mr-2"></i>
            <p class="text-xl"><%=weather.main.temp%>&deg;F</p>
          </div>
          <div class="flex items-center mb-2">
            <i class="fas fa-cloud text-xl text-blue-500 mr-2"></i>
            <p class="text-xl"><%=weather.weather[0].main%></p>
          </div>
        </div>
        <% } %>
      </div>
    </div>
    <script src="https://use.fontawesome.com/releases/v5.14.0/js/all.js"></script>
  </body>
</html>
