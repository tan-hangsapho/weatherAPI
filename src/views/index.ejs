<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Weather App</title>
    <!-- Include Tailwind CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <!-- Include Font Awesome -->
    <link
      rel="stylesheet"
      href="https://use.fontawesome.com/releases/v5.14.0/css/all.css"
    />
  </head>
  <body class="bg-gray-100">
    <!-- Centering content using Flexbox -->
    <div class="flex justify-center items-center h-screen">
      <div class="container mx-auto px-4 py-8 max-w-md">
        <h1 class="text-3xl font-bold mb-4 flex justify-center">Weather App</h1>
        <form action="/weather" method="get" class="mb-8">
          <input
            type="text"
            name="city"
            placeholder="Enter city name"
            class="w-full px-4 py-2 rounded-lg shadow-sm border-gray-300 focus:border-indigo-500 focus:ring focus:ring-indigo-500 focus:ring-opacity-50"
          />
          <button
            type="submit"
            class="mt-2 inline-block bg-blue-500 text-white px-4 py-2 rounded-lg transition duration-300 hover:bg-blue-600"
          >
            Get Weather
          </button>
        </form>
        <!-- Display error or weather data -->
        <% if (error) { %>
        <p class="text-red-500 mb-4"><%= error %></p>
        <% } else if (weather) { %>
        <div class="bg-white rounded-lg shadow-md p-4">
          <!-- Add your weather data here -->
          <p class="flex items-center">
            <i class="fas fa-thermometer-half mr-2"></i>
            <span><%=weather.main.temp%>&deg;F</span>
          </p>
          <p class="flex items-center">
            <i class="fas fa-cloud mr-2"></i>
            <span><%=weather.weather[0].main%></span>
          </p>
          <p class="flex items-center">
            <i class="fas fa-map-marker-alt mr-2"></i>
            <span><%=weather.name%>, <%=weather.sys.country%></span>
          </p>
        </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
